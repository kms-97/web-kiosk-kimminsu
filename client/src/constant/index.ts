export const SIZE_STRING: { [key in string]: string } = {
  s: 'Small',
  m: 'Medium',
  l: 'Large',
};

export const TEMP_STRING: { [key in string]: string } = {
  h: 'Hot',
  c: 'Ice',
};

export const PAYMENT_STRING: { [key in string]: string } = {
  cash: '현금결제',
  card: '카드결제',
};
